# Wrap-Up — 2026-07-28

## Done
- No commits today.
- Beads not available in remote environment.

## Doing
- Sprint 5.2 shipped yesterday (2026-07-27): route path rendering on Leaflet map via OpenRouteService — fetches road-following geometry per route, caches result on route row to stay within ORS free-tier limit.
- Netlify deployment still pending manual platform setup (netlify.toml and build config committed and pushed since 2026-07-20).

## Next
- Create Netlify site: netlify.com → Add new site → Import from GitHub → SLA-AI-COURSE-001.
- Add required env vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_API_URL`).
- After first deploy, set `APP_URL` and update WhatsApp webhook URL, then resume Sprint 1 auth middleware and DB migrations.

## Notes
No notes recorded today. Most recent feature: Sprint 5.2 added `routePath` service (`apps/api/src/services/routePath.ts`) that computes a 10 km origin point per route and fetches ORS geometry through pickup-ordered students to school, with DB caching via migration `20260127000000_add_route_path_geometry.sql`. Map rendering in `RouteMap.tsx` colour-matches route paths to student pins.
