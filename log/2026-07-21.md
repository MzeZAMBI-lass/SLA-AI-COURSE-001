# Wrap-Up — 2026-07-21

## Done
- No commits today.

## Doing
- Netlify site not yet created — all deployment config is committed and pushed; manual platform setup is still pending.
- Sprint 1 work (auth middleware, DB schema migrations, geocoding pipeline) is paused until deployment is live.

## Next
- Create Netlify site: netlify.com → Add new site → Import from GitHub → SLA-AI-COURSE-001.
- Add all env vars (see `.env.example` and `apps/web/.env.local`); set `NEXT_PUBLIC_API_URL=""`.
- After first successful deploy, set `APP_URL` env var and update WhatsApp webhook URL.
- Resume Sprint 1: auth middleware, database schema migrations, geocoding pipeline.

## Notes
No notes recorded today.
