# Wrap-Up — 2026-07-20

## Done
- Switched deployment platform from Vercel + Render to **Netlify** (Decision 4 in `decisions.md`).
- Added `netlify.toml`: build config routes `/api/*` and `/webhook` to a Netlify Function; rest goes to Next.js.
- Added `netlify/functions/api.js`: one-file serverless wrapper using `serverless-http` — Express code unchanged.
- Added `serverless-http` to `apps/api` dependencies; verified API builds cleanly.
- Updated `SKILL.md` deploy row and `decisions.md` with full rationale.

## Doing
- Netlify site not yet created — code is ready and pushed; platform setup is next.

## Next
- Follow the deployment guide (from today's session) to create the Netlify site:
  1. netlify.com → Add new site → Import from GitHub → SLA-AI-COURSE-001
  2. Build settings auto-filled from `netlify.toml`
  3. Add all env vars (see `.env.example` + `NEXT_PUBLIC_*` vars from `apps/web/.env.local`)
  4. Set `NEXT_PUBLIC_API_URL` to empty string `""` (same-domain deployment)
  5. After first deploy, grab Netlify URL, set `APP_URL` env var, redeploy
  6. Update WhatsApp webhook URL to `https://your-site.netlify.app/webhook`
- Once deployed, resume Sprint 1: auth middleware, DB schema migrations, geocoding pipeline

## Notes
- `vercel.json` and `render.yaml` kept in repo for reference but no longer the active deployment config.
- Netlify Function 10s timeout is acceptable for MVP; upgrade to Background Functions if Phase 2
  geocoding+routing exceeds that in a single synchronous request.
- CORS: `APP_URL` env var should equal the Netlify site URL — API and frontend share the same domain
  so CORS headers are technically redundant, but keeping `APP_URL` set avoids any misconfiguration.

---

# Wrap-Up — 2026-07-20 (end of day)

## Done
- Refactored Supabase client into separate browser/server variants for Next.js App Router compatibility.
- Removed superseded `apps/web/lib/supabase.ts` (replaced by `supabase-client.ts`).
- Fixed Netlify build: pinned Node.js 20, simplified build command, corrected `base` directory setting.
- Iterated through three `netlify.toml` fixes to resolve `@netlify/plugin-nextjs` path resolution.

## Doing
- Netlify site not yet created — all config is committed and pushed; platform setup remains.

## Next
- Create Netlify site: import repo, add env vars, set `NEXT_PUBLIC_API_URL=""`, deploy.
- After first successful deploy, set `APP_URL` and update WhatsApp webhook URL.
- Resume Sprint 1: auth middleware, DB schema migrations, geocoding pipeline.

## Notes
No notes recorded today.
