# Wrap-Up — 2026-07-27

## Done
- No commits today.
- Beads not available in remote environment.

## Doing
- Netlify deployment still pending manual platform setup (netlify.toml and build config committed and pushed since 2026-07-20).
- Sprint 1 (auth middleware, DB schema migrations, geocoding pipeline) paused until first successful Netlify deploy.

## Next
- Create Netlify site: netlify.com → Add new site → Import from GitHub → SLA-AI-COURSE-001.
- Add required env vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_API_URL`).
- After first deploy, set `APP_URL` and update WhatsApp webhook URL, then resume Sprint 1.

## Notes
No notes recorded today. Last meaningful work was 2026-07-25: logout button added to the dashboard header (`LogoutButton.tsx`) and fixed to correctly redirect staff to the login page on sign-out — enabling safe device hand-off on shared hardware.
